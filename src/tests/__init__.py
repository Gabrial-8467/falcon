"""
Test suite for the Falcon programming language.

This file intentionally stays minimal — it only marks the `tests` directory
as a Python package so pytest and import paths behave correctly.
"""
