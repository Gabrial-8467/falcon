// Falcon custom error handling style: try/catch + throw

fn safeDivide(a: number, b: number): number {
    when (b == 0) {
        throw "division by zero";
    }
    give a / b;
}

try {
    show("10 / 2 =", safeDivide(10, 2));
    show("10 / 0 =", safeDivide(10, 0));
} catch (err) {
    show("Caught error:", err);
}

try {
    throw "manual failure";
} catch (e) {
    show("Recovered:", e);
}

