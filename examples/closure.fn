function makeCounter() {
    var c = 0;
    function inc() {
        c = c + 1;
        return c;
    }
    return inc;
}

var counter = makeCounter();
show(counter());
show(counter());
show(counter());
